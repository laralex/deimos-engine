// App embedded into engine, to debug things while deloping the engine
// (such way is easier to hot-reload without bookkeeping symbols from the engine)

#include "DevelApp.hpp"

auto StartApplication() -> void {
   return;
}